const assert = require('assert'),
  Bot = require('../lib/bot');

describe('Bot', () => {
  describe('constructor', () => {
    it('should create a blank bot with no arguments', () => {
      let bot = new Bot();
    });
  });
});
